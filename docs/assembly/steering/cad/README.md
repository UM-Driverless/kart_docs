<!-- CAD download files for the steering subsystem. Linked from reducer.md / index.md. -->
# Steering CAD files

Drop exported CAD here; they become download links on the docs site.

- `*.step` / `*.stp` — editable CAD (open in any package)
- `*.stl` / `*.3mf` — printable mesh (gears)
